/**
 * @file catch.cpp
 *
 * Catch is a nice multi-paradigm automated unit test framework for C++,
 * Objective-C (and, maybe, pure C). It is implemented entirely in a set of
 * header files, but is packaged up as a single header for extra convenience.
 *
 * @see https://github.com/philsquared/catch
 * @see http://builds.catch-lib.net/
 */

// this macro tells the Catch to provide a main() function in here
#define CATCH_CONFIG_MAIN

#include "catch.hpp"
